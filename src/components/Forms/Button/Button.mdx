import {
  Meta,
  Title,
  Primary,
  Controls,
  Stories,
  ArgTypes,
} from '@storybook/addon-docs/blocks';

<Meta title="Components/Button" />

<Title />

O componente **Button** é usado para disparar ações dentro da interface.  
Suporta múltiplas cores, tamanhos e variações de estilo, permitindo personalização total com temas.

---

## 🚀 Uso

Para utilizar o componente em seu projeto React:

```tsx
import { Button } from '@hans/ui-design-lib';

export const Example = () => (
  <Button label="Click me" color="primary" variant="default" />
);
```
