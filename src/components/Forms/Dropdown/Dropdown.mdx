import { Meta, Title, Primary, Controls, Canvas } from '@storybook/addon-docs/blocks';
import * as HansDropdownStories from './Dropdown.stories';

<Meta of={HansDropdownStories} />

<Title />

The **Dropdown** component is a button-triggered popup menu.  
It supports option lists by default and custom content through `children`.

## Usage

```tsx
import { HansDropdown } from 'hans-ui-design-lib';
```

<Primary />

## Properties

<Controls />

## Options Menu

Use `options` for the standard dropdown menu behavior.

<Canvas of={HansDropdownStories.WithOptions} />

## Custom Content

Pass any custom content in `children` (including other components).

<Canvas of={HansDropdownStories.CustomContent} />

## Controlled Open

Use `onOpenChange` to observe open/close state.

<Canvas of={HansDropdownStories.ControlledOpen} />

## Loading

Use `loading` to render a loading state inside popup content.

<Canvas of={HansDropdownStories.Loading} />
