import { Meta, Title, Primary, Controls, Canvas } from '@storybook/addon-docs/blocks';
import * as PopupStories from './Popup.stories';

<Meta of={PopupStories} />

<Title />

The **Popup** component centralizes floating panel behavior (outside click, layering and up/down direction).
It is used internally by both **SelectOption** and **Dropdown** components.

## Usage

```tsx
import { HansPopup } from 'hans-ui-design-lib';
```

<Primary />

## Properties

<Controls />

## Custom Content

Use any custom children inside the popup panel.

<Canvas of={PopupStories.CustomContent} />

## Trigger Elements

Popup supports different trigger elements (button, input-like trigger, icon trigger, text trigger).

<Canvas of={PopupStories.TriggerElements} />

## No Content

Use this scenario to validate behavior when no panel content is provided.

<Canvas of={PopupStories.NoContent} />
